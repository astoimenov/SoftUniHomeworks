function variablesTypes (input) {
	/*var name = input[0];
	var nameType = typeof(name);
	var age = input[1];
	var ageType = typeof(age);
	var isMale = input[2];
	var isMaleType = typeof(isMale);
	var foods = input[3];
	var foodsType = typeof(foods);
	jsConsole.writeLine("My name: " + name + " // type is " + nameType);
	jsConsole.writeLine("My age: " + age + " //type is " + ageType);
	jsConsole.writeLine("I am male: " + isMale + " // type is " + isMaleType);
	jsConsole.writeLine("My favorite foods are: " + foods + " //type is " + foodsType);*/
	console.log("My name: " + input[0] + " // type is " + typeof (input[0]) + "\n" + "My age: " + input[1] + " //type is " + typeof (input[1]) + "\n" + "I am male: " + input[2] + " // type is " + typeof (input[2]) + "\n" + "My favorite foods are: " + input[3] + " //type is " + typeof (input[3]));
}

}
